import { Router } from 'express';
import { registrationUser,loginUser } from '../controllers/authController';

const router = Router();
// router.post('/register', registrationUser);
// router.post('/login', loginUser);

export default router;